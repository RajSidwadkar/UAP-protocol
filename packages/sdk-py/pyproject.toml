[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "uap-sdk"
version = "0.1.0"
dependencies = [
    "httpx==0.27.0",
    "pydantic==2.7.4",
    "fastapi==0.111.0",
    "python-ulid==2.0.0"
]
requires-python = ">=3.11"
